apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-service
  namespace: flm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: book-service
  template:
    metadata:
      labels:
        app: book-service
    spec:
      containers:
        - name: book-service
          image: nareshujjineni/flm0307:bookimage
          ports:
            - containerPort: 5002
